//main game logic

/*

int playerChoice, computerChoice, winCheck
playerChoice = getfromform()
computerChoice = randint(1,3)

if playerChoice == computerChoice
    do winCheck=0

else if:
    case: playerChoice==1 && computerChoice==3
        do winCheck=1
            case: playerChoice==2 && computerChoice==1
        do winCheck=1
            case: playerChoice==3 && computerChoice==2
        do winCheck=1

else:
    do winCheck = 2

 */

// use same cookie as login.js to track scores

/*

HTML write playerChoice
HTML write computerChoice
HTML write result

activearray = readCookie()

if winCheck == 1:
    increment playerWinCount
else if winCheck ==2:
    increment computerWinCount

write scores
write cookie

 */